# Demo 检查记录

- TypeScript 检查通过。
- 单批检查第 55、175、290、680、950、1400、1740 帧。
- 对照 filmstrip 参考：素材在左、文字在右；主要内容区域垂直对应；切词时素材步进；镜头之间保留阅读停顿。
- 配色使用 Akari 色值和 Frost 青绿；无 Ember 橘色。
- 检查后修正：开场右侧竖排标注位置、问题段“当想法散落”的语义、成员卡片焦点的连续位移。
- 首次完整渲染因一个页面加载本地 Inter 字体超时失败；降低并发到 2、等待上限增加到 120 秒后重试。
- 重试完整导出成功。ffprobe 确认 H.264、1920×1080、30fps，容器时长 60.053 秒（视频时间线 60 秒），文件约 6.56 MiB。前 26 秒预览从成片直接截取，无额外渲染；已检查成片 5.8 秒处抽帧。
- 真实人像、录音与现场素材尚未提供；本版只验证演示内容下的版式与动画，不能代替现场素材验收。

## 第二轮 · 新拟态回归（2026-09-20）

- 起因：首版只借了 Akari 色值，Console 的新拟态语法（外凸 / 内凹 / 胶囊）没有进片。本轮按设计系统 README 与 `preview/` 卡片重做结构，配方集中在 `src/design.tsx`。
- TypeScript 检查通过。抽帧改为全尺寸（`node review.mjs [帧号…]`），默认 55、175、290、500、710、950、1400、1740；另抽 92/93、352、690、779、1195、1610 看过渡。
- 对照 `shadows-neo` / `badges-chips` / `toggles-segmented` / `channel-cards`：外凸卡片为白高光 + 冷灰低光双色阴影；内凹井、内凹轨、按压态、选中态（内凹 + 3px 主色描边）均按配方放大约 2.5×；胶囊 chip 默认外凸、选中主色 + 内凹；分段控件为内凹轨 + 外凸滑块，编号 Geist Mono。
- 检查后修正：开场换色阈值从 0.5 提前到 0.3（白字在冷瓷上闪帧）；开场滑杆等擦拭结束后再出现；开场右侧蓝条改为擦拭留边，去掉与 wash 的接缝；胶片裁切对齐内凹井边缘；接力旋钮改 in-out 缓动。
- 字体：Latin 从 Inter 换为设计系统自托管的 Geist / Geist Mono（Latin 子集各 ~25 KB），Inter 文件已移除；上一轮渲染超时的 350 KB 字体加载不再存在。
- 完整导出成功。ffprobe：H.264、1920×1080、30fps、容器时长 60.053 秒，文件约 7.0 MiB。从成片抽 3.1 秒（开场擦拭中段）与 23.5 秒（接力旋钮停在"行动"）核对，动画与 Studio 抽帧一致。
- 仍待用户评片：阴影放大倍率、蓝底段的外凸 / 内凹变体、开场蓝底是否保留（设计系统本身不用满幅主色，属于已授权的视频适配）。

## 第三轮 · 接手与通用化（2026-09-21）

- 将原始交接状态作为独立 Git 基线保存；旧 Akari / filmstrip 主片明确降级为可运行基线，不再记作视觉批准。
- 内容入口从 Relay 专属字段改为通用的 event / team / project / story / members / progress；五段 story 表达“问题 → 洞察 → 方案 → 原型 → 价值”，版式名称只描述画面结构。
- 成员卡支持 1–6 人自适应横向排布，进展支持 1–4 条自适应时长；素材支持裁切位置和视频入点，非音乐视频统一静音。
- `DialTest` 的边界改为“旋钮选段后转场离场”，本轮没有修改其视觉细节或强行接入主片。
- 新增现场 preflight，检查内容数量、素材是否存在与常见文字长度风险。
- `npm run check` 与 `npm run preflight` 通过；Remotion 可枚举 `XiHackTeamIntro`（60 秒）和 `DialTest`（8 秒）。
- 检查主片第 55、175、290、500、710、950、1400、1740 帧，以及 `DialTest` 第 190 帧；通用占位文案、五段 story、四人排布和三条进展无溢出或空白场景。
- 完整导出成功。ffprobe：H.264 + AAC、1920×1080、30fps、容器时长 60.053 秒，文件 7,624,757 bytes。
- 生成的 `package-lock.json` 已确认不包含本机相邻仓库路径。`npm audit` 对 Remotion 4.0.441 工具链报告 9 个依赖告警（8 high / 1 critical），自动修复目标为整体升级至 4.0.526；为避免在通用化提交中混入运行时升级，本轮只记录，未执行 `audit fix`。

## 第四轮 · 旋钮主线与真实时间标尺（2026-09-21）

- 完全删除此前生成的团队图形标志及所有场景引用；没有生成替代标志。
- 主片从固定 60 秒调整为暂定 48 秒（1440 帧），六章时长集中在 `src/timeline.ts`。这是等待 BGM 的无声结构剪辑，不是新的硬性时长。
- 旋钮已接入六章开头，分别选择团队 / 项目、问题、方案、我们、今天、下一步；进入内容时缩放、淡出并重叠 10 帧，内容场景内不常驻旋钮。
- 删除旧版每场重复出现的装饰性底栏，新增全片统一的真实时间标尺：刻度位置按章节帧数计算，显示当前章节、播放游标和实际时间码。
- `npm run check`、`npm run preflight` 与 Remotion composition 枚举通过；主片为 1920×1080、30fps、1440 帧，`DialTest` 保留 240 帧。
- 抽检主片第 12、40、80、170、225、270、360、450、500、610、720、780、830、980、1035、1090、1210、1300、1350 帧。修正旋钮退出层遮挡内容、蓝底时间标尺对比度，以及方案章节错误读取 story 起始项的问题。
- 本轮不做完整 MP4 导出：结构会在收到 BGM 后按乐句与关键拍重新定时，避免把一次注定重做的临时节奏当成成片验收。

## 第五轮 · BGM 对拍与旋钮精修（2026-09-21）

- 选用 Alanajordan `Brazilian Tropicalia Instrumental 01` 的约 `00:56.03–01:44.63` 乐段；主片重排为 1458 帧 / 48.6 秒。章节边界同时参考音乐重拍、乐句变化与文字阅读时间。
- 新增 `src/Soundtrack.tsx`，用 `@remotion/media` 统一处理 BGM 裁切、0.25 秒淡入、1.2 秒淡出，以及六次自制旋钮机械点击声。原始 MP3 由 `.gitignore` 排除，不进入仓库。
- 旋钮从软质大面板收紧为较硬朗的 hi-fi 档位控件，保留六档与待机位；各章节采用不同的缩放 / 横移 / 纵移离场。旋钮层不再带重复页眉，6 帧交叠在前半段完成透明度退出，避免与正文标题长时间重影。
- 时间标尺显示到 0.1 秒，终点为 `00:48.6`；章节刻度、游标、声音提示与场景均由同一份 timeline 计算。
- `npm run check`、`npm run preflight`、`git diff --check` 与 composition 枚举通过。抽检主片第 12、32、40、42、44、100、265、278、284、286、288、289、465、480、489、490、491、782、796、806、807、808、1042、1056、1066、1067、1068、1302、1313、1320、1321、1322、1435 帧。
- 完整导出成功。ffprobe：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、容器时长 48.618667 秒，文件 6,621,177 bytes。EBU R128：约 −17.8 LUFS integrated、1.6 LU LRA、−7.9 dBFS true peak。
- 从最终 MP4 本身抽取 1.40、8.95、9.53、16.34、26.90、35.57、44.03、48.10 秒画面复核；旋钮、章节转入、蓝底场景、收尾和真实进度标尺均与预期一致。
- 当前仍是演示占位内容，不构成现场项目名称、成员、问题、方案和素材的最终验收。

## 第六轮 · 四人锁定与交叉评审落地（2026-09-21）

- 新增 `docs/OPTIMIZATION-PLAN.md`，把交叉评审建议分为内容/媒体可靠性、四人场景、旋钮时序和最终验收四组，并按文件持续勾选。队伍人数锁定为四人；不再维护 1–6 人分支。
- `content-validation.mjs` 成为 Studio 与 preflight 的共用契约：五段故事、每段三点、四名成员、1–4 条进展、非空可见文字、媒体对象和音乐数值现在会得到一致判断。
- `timeline-config.mjs` 成为浏览器、声音、标尺、预检和 review 脚本共用的时序源。旋钮仍只交叠 6 帧，但空间运动提前 5 帧启动；点击声、按压峰值、LED 和标尺刻度使用同一帧定义。
- 开场/收尾项目名、团队名和 Story 关键词改为真实字体测宽。实测 11 字中文项目名与 34 字英文项目名均保持单行，收尾未与 `05` 重叠；英文标题负字距由固定 −16px 收敛到 −4px，避免小字号时字母互相覆盖。
- 四人章固定四张 405px 卡，姓名和职责常驻对应卡片，焦点只承担强调。`demo:false` 时右下角戳和各场编辑提示同时消失。
- “今天”支持四条进展的 10 帧快速入场，并把原有 35 帧收束擦除变成 1.17 秒总结画面；不增加全片时长。
- 用 10 秒、30fps、画面直接显示源帧号的 H.264 测试视频验证局部时钟：第二个 Story 条目在全片第 391 帧显示源帧 `48`，第二条 Progress 在第 1132 帧显示源帧 `49`，均等于 1 秒入点加各自条目约 18–19 帧，而不是沿用整章时间。
- 音频边界实测：`fadeIn=0` / `fadeOut=0` 的 31 帧短渲染成功；`30s + 30s` 会由 preflight 以“超过 48.60s”明确拒绝。当前默认 BGM 经 ffprobe 为 151.18 秒，足以覆盖 56.03 秒入点后的完整主片。
- 临时测试视频只用于上述验收，随后从 `public/` 删除；默认 `content.json` 已恢复，BGM 继续由 `.gitignore` 排除。
- 最终旋钮交接保持 6 帧正文交叠，但旋钮空间运动提前、章节大字先退场、外层透明度稍早衰减。逐帧复核第 282 / 284 / 286 帧：旋钮方向可读，问题正文在第 284 帧开始接棒，没有重新引入巨型双标题重影。
- 最终完整导出成功：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、容器时长 48.618667 秒，文件 6,843,392 bytes，SHA-256 `30FBB07FEF645C2BCD0448B28D04E524461BFB032D0C77C7AA2B4A6C8E41DBE8`。
- 最终 MP4 的 EBU R128 检查：约 −17.96 LUFS integrated、3.50 LU LRA、−7.88 dBTP true peak；没有削波风险，也不需要为了追求更响再压缩动态。
- 从最终 MP4 本身抽取 1.07 秒点击、9.47 秒问题交接、30.00 秒四人章、42.90 秒进展总结和 46.60 秒收尾复核；关键帧与 Studio 验收一致。

## 第七轮 · 时间标尺中文字体修复（2026-09-22）

- 修复 `ChapterTimeline` 将整条标尺继承为 Geist Mono、导致中文章节名回落到 Chromium 系统等宽字体的问题。编号 `00–05` 与时间码继续使用 Geist Mono；章节中文标签显式使用项目既有的 Geist / Source Han Sans SC 字体栈。
- 第 900 帧静帧与最终 MP4 的 30.00 秒抽帧均已复核：`团队 / 项目、问题、方案、我们、今天、下一步` 为 Source Han Sans SC，粗细与正文一致，刻度位置、间距和时间码未改变。
- 重新完整导出成功：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、容器时长 48.618667 秒，文件 6,834,156 bytes，SHA-256 `C06BBA50C4B301CE11E1E672657528EB80218708073E36F8029AFDEFF9D99BE2`。

## 第八轮 · 分章节文字动效与稀疏 2.5D（2026-09-22）

- 用户把“异向飞入 / 扇形展开”明确为创意启发，而非必须逐字复刻的规格。本轮据此为不同阶段分配不同运动性格，同时保留开场、四人章和收尾作为安静对照。
- 问题章的 lead / keyword / description 分别从上方、右侧和下方短暂聚拢；方案章采用共用左轴的克制铰链 / 扇形错帧；今天章只给编号、标题和描述加入浅景深 CSS perspective、rotateX/Y 与 translateZ。所有稳定态均回到原布局和零旋转。
- 没有改变六章时长、旋钮交叠、内容字段、媒体局部时钟、方案末段扩张、四条进展总结或字体；没有引入 Three.js、WebGL 或新依赖。
- `npm run check`、`npm run preflight` 与 `git diff --check` 通过。检查了问题章 284 / 291 / 304 帧、方案章 486 / 494 / 507 帧、今天章 1063 / 1071 / 1084 帧，并从最终 MP4 连续抽取每组三段各六帧检查运动过程；未见跳变、裁切或文字碰撞。
- 生成 `out/text-motion-preview.mp4`，把问题、方案、今天三种新入场各截取 1.5 秒并按正常速度串联，供设计者单独判断节奏。该预览的生成不等于审美批准。
- 最终完整导出成功：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、容器时长 48.618667 秒，文件 7,181,794 bytes，SHA-256 `44E7B5FB8A7F8B0BE1354AB077238BAA0A871E27AA0166A2ECC64DA218C24A4B`。
- 从最终 MP4 抽取第 304、900、1084、1400 帧，并以全分辨率复核第 304、1400 帧。Source Han Sans SC 覆盖中文正文和标尺章节名，Geist 覆盖 Latin，Geist Mono 覆盖编号与时间码；中文、英文、数字和标点均未观察到非预期 fallback。
- 新增 `docs/WORKFLOW-IMPROVEMENTS.md`，把本窗口反复验证出的 Reference Lock、章节动作词、内容契约、统一时间源、最小验收梯和最终载体字体检查整理为工作流提案；尚未跨仓库修改全局 skill。

## 第九轮 · 70 分基线停板与跨窗口交接（2026-09-22）

- 用户以正常速度观看后把当前版本评价为 70/100：可以作为后续继续加工的可靠基线，但不是最终视觉批准，也不进入 gold 范例库。
- 当前停止继续堆叠特效；下一轮应从真实内容适配、旋钮到正文的因果接力，以及三种现有文字动效的局部力度校准中选择最高收益项，而不是重启配色或视觉方向。
- 新增 `docs/HANDOFF.md`，记录精确基线 commit、最终 MP4 元数据与哈希、本地 BGM、已锁定/拒绝方向、下一轮优先级和最小恢复命令。
- 全局 `yoru-agent-skills` 的 Remotion 入口已加入完整成片生产工作流、跨窗口交接与最终载体字体检查；对应提交为 `60ee0ce`。

## 第十轮 · 下一轮视觉反馈登记（2026-09-22，未改成片）

- 用户指出当前新拟态 / 拟物主要集中在按钮与旋钮，其他大面积载体仍偏扁平；下一轮需要扩展整套材质层级，而不是机械加重所有阴影。
- 用户指出旋钮每次重新进入时缺少过渡。下一轮需要补“正文退出 → 旋钮进入”的动作语言，并可在统一家族内采用不同类型。
- 用户认为问题、方案、今天三种新文字动效时间太短。下一轮先以 24–32 帧为测试区间，并保留稳定阅读时间；该帧数是实现假设，不冒充用户指定值。
- 本轮只更新 `REFERENCE-LOCK`、`HANDOFF` 与 Phase 6 清单，没有修改视觉代码或重新渲染；SHA-256 为 `44E7B5FB8A7F8B0BE1354AB077238BAA0A871E27AA0166A2ECC64DA218C24A4B` 的 70 分成片仍是当前基线。

## 第十一轮 · 材质层级、旋钮入场与文字时长（2026-09-22）

- 用户授权在当前窗口直接落地第十轮三项反馈，并明确人物章节只是占位：四张人物卡及人物章材质保持原样，不参与本轮材质深化。
- `src/design.tsx` 新增共享 `MaterialPlate`，同时强化 `Card` / `Well` 的渐变表面、高光边、接触阴影和内凹暗部。开场、问题 / 方案、今天和收尾现在拥有一致的 raised / inset 承载关系；没有用全局加深阴影替代材质层级。
- `src/timeline-config.mjs` 新增 10 帧旋钮入场窗口。每个非首章的 Dial 在原章节边界开始，上一章末帧通过 `Freeze` 继续承载这 10 帧，使横向、纵向或景深缩放入场能够从正文中长出来；原有章节时长、点击帧和 6 帧“旋钮 → 正文”交叠均未改变。
- 问题 / 方案的 label、lead、keyword、description、segmented 入场窗口延长到约 15–39 帧；今天的编号、标题、描述与 segmented 延长到约 23–39 帧。三种动作性格、稳定排版、媒体局部时钟和后续阅读停顿均保留。
- 正常速度预览为 `out/v2-motion-preview.mp4`：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、9.728 秒、1,828,157 bytes。该文件串联问题、方案和今天的旋钮入场与较长文字动作，用于快速评片。
- `npm run check`、`npm run preflight` 和 `git diff --check` 通过；preflight 确认 5 段 story、4 名成员、3 条进展、0 warning，BGM 151.18 秒。
- 完整导出成功并通过 `ffmpeg -v error -f null` 全片解码：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,256,448 bytes，SHA-256 `FAFDAA7440CAB62FC838AE1CAA52933A476F78E5B1A64AD3C989764187A56FA6`。
- 从最终 MP4 本身抽取第 70、265、330、467、950、1120、1304、1380 帧，复核开场材质、三处旋钮入场、Story、人物占位、今天和收尾。人物场景保持原外观；其余目标场景未见裁切、重影或层级错位。
- 最终载体字体复核：中文正文与标尺章节名由 Source Han Sans SC 承载，Latin 由 Geist 承载，编号与时间码由 Geist Mono 承载；数字、中文 / 英文标点和粗细均未观察到非预期系统字体 fallback。
- 原 70/100 成片已另存为 `out/xihack-team-intro-70-baseline.mp4`，哈希仍为 `44E7B5FB8A7F8B0BE1354AB077238BAA0A871E27AA0166A2ECC64DA218C24A4B`，未被新版覆盖。

## 第十二轮 · 新拟态完整载体审计与开场减法（2026-09-22）

- 用户要求找出所有仍像“扁平色块”的地方并改为新拟态，同时删除开场底部“一起，把想法往前推一步。”。本轮先在 `REFERENCE-LOCK` 中把“色块”限定为承载信息、选择状态或占位素材的可见面；页面 wash、满幅蓝底、时间标尺细线、连接线、状态点和真实媒体不被误改成装饰卡。
- 延续上一轮的明确例外：人物章及四张占位人物卡没有进入材质改造。第 950 帧与上一版结构一致，等待真实成员照片后再决定最终构图和材质。
- `src/design.tsx` 新增共享的浅瓷、抬升、内凹、蓝色、青绿和浅色材质面，并为 Card / Well / Pill / Segmented / Track / Tile 统一补足表面受光、接触阴影和按压关系。Story 的五种 fallback 画布、Today 的 Sketch / 步骤块 / 总结行和 `DialTest` 显示面均进入同一材质系统。
- 源码背景审计后，剩余直接纯色背景只属于环境面、功能细线、实体旋钮细节或已批准的人物占位例外。开场底部句子已直接删除，没有补另一句口号；第 70 帧确认留白自然。
- 中间载体完成并通过全片解码：H.264 + AAC、1920×1080、30fps、48.618667 秒、8,390,085 bytes，SHA-256 `D781F0928E9582540623E87F8A75D1C981DBE3435E0CBEDBE92E57C21AD8B3A3`。对应视觉实现提交为 `4260a51`。

## 第十三轮 · 内凹优先层级（2026-09-22）

- 用户明确个人偏好为 **内凹 > 外凸**。本轮把它解释为材质层级而非单一效果：大面积展示窗、信息井、列表槽、阶段承载面和持久语义标签优先 inset / sunken；便签、媒体卡、旋钮、活动滑块等可拿取或可操作前景继续 raised / lifted。
- 大型 `MaterialPlate` 从外凸板改为机身内嵌显示窗；Story fallback 画布、普通 / 选中列表行、steps 阶段块、Today 的 Sketch / 三格步骤 / 总结行与语义 Pill 使用不同强度的内凹。页面平面、功能线以及四张人物占位卡保持不变。
- 同一偏好已写入 `yoru-and-akari Console Design System` 的 `SKILL.md` 与 `README.md`，包含使用范围和“不能把所有像素都压下去”的约束；设计系统提交为 `834b075`，已推送。
- `npm run check`、`npm run preflight` 与 `git diff --check` 通过；preflight 确认 5 段 story、4 名成员、3 条进展、0 warning，BGM 151.18 秒。
- 最终完整导出并通过 `ffmpeg -v error -i ... -f null NUL` 全片解码：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,352,412 bytes，SHA-256 `7A904D5D7BEE11F45E89F19660E1485CFA08E31317F0E09E8832CBCC992106C7`。对应视觉实现提交为 `7e7cc68`。
- 从最终 MP4 本身抽取第 70、330、540、950、1120、1295、1380 帧进行全分辨率复核。内凹方向在蓝底与浅底均清晰，人物占位保持原样，开场已无被删除句子。中文由 Source Han Sans SC、Latin 由 Geist、编号与时间码由 Geist Mono 承载；中文 / 英文标点、字重和标尺标签均未观察到非预期 fallback。
- 当前仍是演示占位内容；本轮确认的是材质偏好落地与技术载体可靠性，不冒充用户对新版整体观感的最终评分。

## 第十四轮 · Today checkpoint 与章节标尺交接修复（2026-09-22）

- 用户正常观看当前候选后给出约 **78/100** 的阶段评价，并通过截图指出 Today 总结画面疑似异常。源码和边界帧确认这不是播放器故障：总结板固定在 `top: 492`，形成无叙事作用的巨大顶部空白；下一章又在其 10 帧旋钮入场尚未可见时提前高亮 `05 下一步`。
- `src/scenes/Field.tsx` 将同一块 1776×410 inset checkpoint 从画面下部移到中部，没有新增装饰或另一套材质。原有 35 帧总结预算不变，改为约 20 帧 reveal + 15 帧完全停稳阅读；全片仍为 1458 帧。
- `src/ChapterTimeline.tsx` 只把当前章节的高亮交棒延迟 10 帧，与共享 `DIAL_INGRESS_FRAMES` 对齐。真实进度线、游标、时间码、背景配色和点击反馈仍在实际帧上推进，没有把整个时间标尺伪造为慢 0.33 秒。
- 源码关键帧 1264 / 1274 / 1284 / 1295 / 1299 / 1304 / 1309 / 1313 与 1254–1334 正常速度短预览通过：第 1299 帧仍为 `04 今天`，第 1309 帧旋钮完整出现时切换 `05 下一步`。
- `npm run check`、`npm run preflight`、`git diff --check` 通过；preflight 明确报告每项进展约 2.23 秒、0.67 秒总结 reveal、0.50 秒 settled hold，0 warning。
- 最终 MP4 通过整片解码：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,305,733 bytes，SHA-256 `9E69E176C2DEACED260D0E60391A6572D8C62E4074391C61692909136FCB5F8B`。视觉实现提交为 `8b3915f`。
- 从最终 MP4 本身抽取第 1295 / 1299 / 1309 / 1400 帧复核。checkpoint、标尺高亮、旋钮与收尾均无裁切或状态错位；中文由 Source Han Sans SC、Latin 由 Geist、编号与时间码由 Geist Mono 承载，未观察到非预期 fallback。
- 本轮只修复截图暴露的边界问题，不把 78/100 阶段评价描述为最终视觉批准。完整 MP4 通过 GitHub prerelease `review-candidate-2026-09-22` 提供给外部 AI 交叉评审，原始 BGM 仍不进入 Git。

## 第十五轮 · 旋钮因果接力与四／五人双版本（2026-09-23）

- 先把两份交叉评审拆成接受、拒绝和少量待定，并在 `REFERENCE-LOCK` 中记录后再改代码。接受项包括：方案章的旋钮 → 正文几何接力、单一运动引擎、稳定阅读保持、克制的两段按压，以及 `demo=false` 正式出片门；扫描线、时间尺走带表演、默认静音和人物占位材质深化未采用。
- `DialCue(solution)` 的青绿标题线在既有 6 帧 overlap 前先收成短段，再移动、旋转并落成 Story 右侧纵轴；Story 的 lead / keyword / description / segmented 由同一个 38 帧 engine 分阶段响应。第 479 / 481 / 483 / 485 / 486 / 500 / 524 帧与正常速度短预览确认接力连续，稳定态仍有阅读保持。
- 旋钮 press 改为 contact → trigger → release 三段组合，但 `dialClickFrame()`、音效触发和章节边界未移动。
- `content-validation.mjs` 新增 `demo=false` 占位文本硬门；preflight 会输出目标版本、团队、项目、成员、五段 story、progress、所有视觉媒体和音乐摘要。用占位内容模拟 `demo=false` 时能稳定得到 17 项错误，不再允许“技术可渲染但仍是演示文案”的成片通过生产检查。
- 四人版继续使用 `XiHackTeamIntro` 与 `content.json`。新增 `XiHackTeamIntro5P` 与 `content-5p.json`；五人版只覆盖成员名单，并专门使用 320px 卡宽、22px 间距、16px 职责文字和 10 帧 focus transition，未改变占位肖像材质、全片时长或其他五章。
- 五人 People 章先渲染 800–1038 帧正常速度预览，并抽检五次成员聚焦与团队合流。检查发现旧的成员身份与合流文案交叉淡化会形成重影，随后改成 12 帧退出、10 帧后再以 18 帧接入；第 969 / 975 / 981 / 987 / 993 / 999 帧确认两者不再叠字。
- `npm run check`、`npm run preflight`、`npm run preflight:5p` 与 `git diff --check` 通过；composition 枚举为 `XiHackTeamIntro`、`XiHackTeamIntro5P`、`DialTest`。
- 两条最终 MP4 均通过 `ffmpeg -v error -i ... -f null NUL` 全片解码，并从最终载体抽取方案接力、四／五人聚焦与合流、Today、checkpoint 和 closing 帧进行复核。中文、Latin、数字、中文／英文标点、粗细与 mono 角色均未观察到非预期 fallback。
- 四人版：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,128,744 bytes，SHA-256 `AE2ACADFB5FB9AB3B47835E7CC884C68E6383FAE88E91CB4BCDDBBD155202341`。
- 五人版：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,248,967 bytes，SHA-256 `929CF9D47D153DABF1A96B4EC3E0911E832190A94BB072EDE2BB22FB57F1DAF7`。
- 本轮完成的是已批准建议的落地与双版本工程交付；真实成员照片尚未提供，人物卡继续被视为可替换占位，不把五人版当前肖像观感当作最终人物章批准。

## 第十六轮 · 正文收回旋钮（2026-09-23）

- 一位人类观众正常观看后指出：每张内容页回到下一枚旋钮时仍像没有动画。最终 MP4 连续帧与代码确认旧实现只冻结上一页，再用 10 帧让新旋钮页淡入；上一页本身没有退场运动。用户批准给四人 / 五人版都加入类似 Mac 窗口最小化的定向收拢。
- 先把用户指定的参考与 18 帧实现假设记录在 `REFERENCE-LOCK.md`。随后将前一章实际末帧放进当前 `DialCue` 的 `ReturnWindow`：0–4 帧轻微脱离，之后以逐帧、非等比缩放和短弧位移走向旋钮中心，最后几帧才淡出。新选择器从下方显现；首次开机没有上一页，因此不套该动作。
- 五个正文返回共用一套实现；四 / 五人 composition 的人员布局、音乐点击帧、六章时长、旋钮到正文的 6 帧 overlap 和场景稳定状态未改。底部时间尺的真实游标、时间码和刻度继续按实际帧走，只有章节名称在返回结束后切换。
- 初版小样发现中段缩得过急、旋钮透过旧页形成重影；将曲线调得更均匀，并让旧页处于旋钮之上的实体层。四人 Opening → Problem、四人 Today → Next、五人 People → Today 三段短预览和连续帧均确认了完整页到旋钮的可追踪路径；这是逐帧与短预览验证，不冒充用户对正常速度全片的审美批准。
- `npm run check`、`npm run preflight`、`npm run preflight:5p` 与 `git diff --check` 通过；两版 preflight 均为 0 warning。两条完整 MP4 均通过 `ffmpeg -v error` 全片解码。最终载体的第 259–285 / 1038–1064 帧确认交接、五人占位卡整体退场和标尺高亮；第 1380 / 950 帧复核中文、Latin、数字、标点、字重与 mono 字体角色，没有观察到非预期 fallback。
- 四人版：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,995,652 bytes，SHA-256 `1E139C3816F8A0B6433BE3056B5528E2F42233336CFE2958F1498E3F0EC5084A`。
- 五人版：同规格与时长、9,095,007 bytes，SHA-256 `D6CB311A0EE394B04A6C9A1C28EF6876DB5503AA2230D933EABD60AC9FC81BCD`。
- 两版新成片发布在 `scene-return-candidate-2026-09-23` prerelease。上一版 `*-optimized.mp4` 与对应 prerelease 保留，便于直接 A/B；后续应以用户正常速度观看新成片后的具体反馈决定是否再调收缩时长或运动形状。

## 第十七轮 · Today 三段节奏去重复（2026-09-23）

- 用户正常观看后指出「今天，向前一步。」的三段进展动效重复，并同意「锁定信息 → 展开素材 → 收束接 checkpoint」的定向优化。实施前已在 `REFERENCE-LOCK.md` 记录现有母版、获准方向与 keep 镜头的借用边界。
- `Field.tsx` 的三段不再共用同一种草图排列和编号／标题／描述 2.5D 入场。第一段保留三格锁定；第二段放大中间证据槽，让媒体卡与右侧文字沿同一展开方向到位；第三段改为逐行结果，运动更轻，并交给原有 checkpoint 总结窗。
- 每次换项短暂保留上一张内容卡与右侧文字，再让下一张接管，避免旧版整块空白闪帧；已有媒体的上一项固定在其条目末帧，新项仍从自己的本地时间起点播放。演示无媒体分支完成视觉验收，真实照片／录屏裁切仍待现场素材。
- 三条进展、每段约 67 帧、原有 20 帧总结 reveal 与 15 帧 hold、1458 帧总长、BGM 点击、底部真实时间尺、四／五人构图均未改变。
- `npm run check`、四／五人 preflight、`git diff --check` 通过。导出 Today 短预览并检查三段入场、稳定态、两处换项和 checkpoint；受本地视频浏览器安全策略限制，agent 未能在浏览器中正常速度播放短片，不将抽帧检查冒充人类审美批准。
- 四人版：H.264 + AAC、1920×1080、30fps、48 kHz 立体声、48.618667 秒、8,796,642 bytes，SHA-256 `1D87BFA7C7B7D7EEA5FF825D077B7BFBA28315BE4B0B4D3164E862198E7172FA`。
- 五人版：同规格与时长、8,895,856 bytes，SHA-256 `79DB53F3CE5F59BF9B6912D4267A735B0A57C11BFFF471B80A27446C5CA249DC`。两条最终 MP4 均完成全片解码，并从最终载体抽帧检查 Today 三段、checkpoint、五人占位和中英文字形、数字、标点、mono 角色；未观察到非预期字体 fallback。
- 两条成片将作为 `today-rhythm-candidate-2026-09-23` prerelease 的独立 assets 提供；前一版 `scene-return-candidate-2026-09-23` 保留供 A/B。最终节奏仍需用户正常速度观看后确认。
